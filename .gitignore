# Project Excludes
node_modules/
dist/

# Flutter app SOURCE tree — kept on the `main` branch and the
# archive/deploy-full-20260615 tag, NOT on the deploy branch (GitHub Pages
# would otherwise publish ~3.5GB of source + native scaffolding). Rebuild
# mobile/ from this local tree; only the compiled mobile/ is committed to deploy.
/EMG_Mastery_Flutter/
.DS_Store
_archive/
_legacy_archive/
backups/
Archive/
backup-module-extraction-20251118/

# Mobile / Capacitor (the whole ios/ Capacitor wrapper is build output)
/ios/
ios/App/App/public/
ios/App/Pods/
ios/App/.bundle/
# NOTE: mobile/ IS tracked on this deploy branch — GitHub Pages serves the
# compiled Flutter web build from it. Rebuild with `flutter build web
# --base-href /LEARNEMG/mobile/` from EMG_Mastery_Flutter/ and rsync
# build/web/ into mobile/ before committing a deploy.

# Exclude development and backup files
emg_ncs_resident_tool.html
*.log
*.txt
*.py
*.apkg
*.webloc
*.command
*.db
*.pdf
*.bak
*.tmp
*.legacy.js
*.backup.js

# Exclude specific directories
EMG_flashcards/
Universal Scheduler/
SCH APP/
SVG/
Photos-*/
Medical texts/
BACKUP_PRE_LEGACY_REMOVAL/
tasks/
trash/
large_files/

# Local agent / editor artifacts
.claude/worktrees/
EMG_Mastery_Flutter/.claude/
**/.mcp.json

# Tempoary Files
temp_*.html
lumbo_backup_temp.js
ncs_backup_temp.js
audio_audit.mjs
construct_patho.js
preston_shapiro_chapters.md
index.html._bak
# Convenience symlink -> EMG_Mastery_Flutter/assets/Podcasts (local only)
/Podcasts
